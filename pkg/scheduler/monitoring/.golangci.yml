run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - gofmt
    - goimports
    - revive

output:
  formats:
    - format: colored-line-number

linters-settings:
  errcheck:
    exclude-functions:
      - fmt.*
      - io/ioutil.ReadAll

issues:
  exclude-use-default: false
